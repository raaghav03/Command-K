// app.ts
import CommandInput from './CommandInput';

const commandInput = new CommandInput("search more","<Search />");
commandInput.render('container-id'); // Replace 'container-id' with the actual ID of your container div
